<h1><%= @project.name %></h1>

<div class="project_link"><%= link_to 'メッセージ設計', messages_path(:project => @project) %></div>

<div class="project_link"><%= link_to 'DB管理',  database_manages_show_path(:project => @project) %></div>

<div class="container">
  <ul class="list-inline pull-right">
    <li>エクスポート：</li>
    <li><%= link_to "ARXML", project_export_path(project_id: @project.id), :target=>["_blank"] %></li>
  </ul>
</div>
